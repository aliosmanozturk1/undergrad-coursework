#include <stdio.h>

void zaman(int n);

int main()
{
    printf("Programın başındasınız lütfen biraz bekleyiniz, program yükleniyor.\n");
    zaman(50000);
    printf("Program başlamıştır\n");
    return 0;
}

void zaman(int n)
{
    int i, j;
    for(i = 0; i < n; i++)
    {
        for(j = 0; j < n; j++)
        {

        }
    }
}